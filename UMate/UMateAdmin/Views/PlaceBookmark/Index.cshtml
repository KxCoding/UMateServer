@model IEnumerable<UMateModel.Entities.Place.PlaceBookmark>

@{ ViewData["Title"] = "Index"; }

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Place)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InsertDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.UserId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Place.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.InsertDate)
    </td>
    <td>
        @*<a asp-action="Edit" asp-route-id="@item.PlaceBookmarkId">Edit</a> |*@
        <a asp-action="Details" asp-route-id="@item.PlaceBookmarkId">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.PlaceBookmarkId">Delete</a>
    </td>
</tr>
        }
    </tbody>
</table>

